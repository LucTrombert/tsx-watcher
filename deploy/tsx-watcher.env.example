# Copy this to deploy/tsx-watcher.env and fill in real values, then:
#   chmod 600 deploy/tsx-watcher.env
# tsx-watcher.env is gitignored — it must NEVER be committed.
#
# These are the same secrets currently stored as GitHub Actions secrets.

TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
ANTHROPIC_API_KEY=
